---
- name: Restart ixp_portal
  become: true
  ansible.builtin.command:
    cmd: docker-compose restart
    chdir: "{{ ixp_portal_install_dir }}"

- name: Reload nginx
  become: true
  ansible.builtin.systemd_service:
    name: nginx
    state: reloaded
