# Managed by Ansible (snmp_exporter role). Read-only NetBox access for the
# Prometheus SNMP target syncer.
NETBOX_API_ENDPOINT={{ ixp_netbox_api_endpoint }}
NETBOX_API_TOKEN={{ snmp_exporter_netbox_api_token }}
