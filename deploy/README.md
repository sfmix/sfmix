# Deploy SFMIX Configuration

SFMIX-related deploy configuration and tools.
Mostly Ansible.

## TODO

- (maybe?) Push !!-style block-level comments for interface descriptions (with patch panel information)
- IP Interfaces (ptp, loopbacks, etc.)
