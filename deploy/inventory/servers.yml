---
all:
  hosts:
  children:
    servers:
      children:
        netbox:
        metrics:
        teleport:
        portal:
        sentry:
        shell:
        dns:
        rs_linux:
    netbox:
      hosts:
        netbox.sfo02.sfmix.org:
    metrics:
      hosts:
        metrics.sfo02.sfmix.org:
    teleport:
      hosts:
        teleport.sfmix.org:
    portal:
      hosts:
        portal.sfmix.org:
    sentry:
      hosts:
        sentry.sfmix.org:
    shell:
      hosts:
        shell.sfmix.org:
    dns:
      hosts:
        ns1-new.sfmix.org:
    rs_linux:
      hosts:
        rs-linux.sfmix.org:
          ansible_host: mgmt.rs-linux.sfmix.org
