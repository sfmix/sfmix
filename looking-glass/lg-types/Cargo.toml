[package]
name = "lg-types"
version = "0.1.0"
edition = "2021"
license = "BSD-2-Clause"
description = "Shared types for the looking glass frontend/backend split"

[dependencies]
serde.workspace = true
serde_json.workspace = true
chrono = { workspace = true, features = ["serde"] }
